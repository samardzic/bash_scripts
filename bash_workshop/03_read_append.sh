#!/bin/bash

# Display message for the user
echo -e "Enter the text that you want to APPEND"

# Store user entered text to Variable "some_text"
read some_text

echo -e "Appended text ${some_text} and the rest"
